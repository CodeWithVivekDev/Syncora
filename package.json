{
  "name": "syncora-connect",
  "version": "1.0.0",
  "description": "Syncora Connect — Real-time YouTube watch party",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start:backend": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev"
  },
  "workspaces": ["backend"]
}
