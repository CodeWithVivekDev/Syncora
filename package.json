{
  "name": "synctube-connect",
  "version": "1.0.0",
  "description": "SyncTube Connect — Real-time YouTube watch party",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start:backend": "cd backend && npm start",
    "dev:backend": "cd backend && npm run dev"
  },
  "workspaces": ["backend"]
}
